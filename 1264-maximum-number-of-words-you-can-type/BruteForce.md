Intuition: Brute Force
- Split the text into each word
- set the counter (number of words) = array size
- Find if a word contains broken letters
- Reduce the counter if the array is oversized.

Approach
Complexity
Time complexity:
- O(n*k), where k = number of broker keys.
worst case O(n * n)

Space complexity:
O(1)
